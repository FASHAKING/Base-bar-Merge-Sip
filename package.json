{
  "name": "merge-sip",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Merge Sip — a beach-bar drink-merge shuffleboard game, built as a Base Mini App",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@farcaster/miniapp-sdk": "^0.1.10"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
